<% provide( :title, @user.name )%>
<p id="notice">
	<%= notice %>
</p>
<div class="row">
	<aside class="span4">
		<section>
			<h1> <%= gravatar_for(@user)%>

			<%= @user.name %>
		</section>
	</aside>
</div>
<%= @user.email %> </h1>

<br>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
